name: pims_deped
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  uuid: ^4.5.0
  sqflite_common_ffi: ^2.3.0
  data_table_2: ^2.5.8
  intl: ^0.18.0

  # Required for stable DB file path on Windows
  path_provider: ^2.1.2
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true